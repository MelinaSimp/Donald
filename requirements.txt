# Core libraries (security/, donald/) are dependency-free standard library.
# These are for running the gateway server and the test suite.
# Shared across the experiments living in this repo.
anthropic>=0.40.0
httpx>=0.27
fastapi>=0.110
uvicorn[standard]>=0.29
pytest>=8.0.0
pytest-asyncio>=0.23

# Prism (head-of-design agent) core:
pyyaml>=6.0
# Prism optional live integrations (install as needed):
# google-genai>=0.3   # Tier 5 images (needs GEMINI_API_KEY + billing)
# fastapi>=0.110      # serve mockups
# uvicorn>=0.29
