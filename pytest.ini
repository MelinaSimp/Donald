[pytest]
testpaths = tests
addopts = -q
# tests/ carries an __init__.py, so `from conftest import ...` needs tests/ on
# sys.path for the shared fakes to import as a top-level module.
pythonpath = tests
# The gateway's segment-stream tests are async; run them without per-test markers.
asyncio_mode = auto
