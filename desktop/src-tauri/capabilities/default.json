{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Donald OS window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "shell:allow-open"
  ]
}
