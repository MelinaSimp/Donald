# Tier 3 voice dependencies. Install on top of requirements.txt when you're
# ready to add push-to-talk:  pip install -r requirements-voice.txt
#
# These pull in PortAudio bindings; on Linux you may need the system package
# first:  sudo apt-get install portaudio19-dev
sounddevice>=0.4.6      # microphone capture + playback (push-to-talk)
numpy>=1.24             # audio buffers
deepgram-sdk>=3.0.0     # ears  (speech-to-text)
elevenlabs>=1.0.0       # mouth (text-to-speech, streaming)
